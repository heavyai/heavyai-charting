import { expect } from "chai"
import * as dc from "../index"

describe("Base Mixin", () => {
  describe("constructor", () => {
    it("should mixin a base chart", () => {
      dc.baseMixin({})
    })
  })
})
