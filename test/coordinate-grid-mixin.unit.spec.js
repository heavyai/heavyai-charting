import {expect} from "chai"
import * as dc from "../src"

describe("Coordinate Grid Mixin", () => {
  describe("constructor", () => {
    it('should create a chart', () => {
      dc.coordinateGridMixin({})
    })
  })
})
